(D) 2026-04-29

### Added
- Added a shared authenticated request retry helper for notification and reminder API calls.

### Changed
- Updated dashboard hero stats to show live notification and reminder totals instead of placeholder metrics.
- Changed the `About`, `Contact`, and `Gallery` page backgrounds to use the same dark gradient style shown in the profile experience.
- Updated the `About`, `Contact`, and `Gallery` cards from solid dark panels to softer blue-slate glass styling.

### Fixed
- Fixed notification dashboard stats so auto-dismissed notifications are not counted as read unless the user explicitly reads them.
- Fixed notification and reminder surfaces becoming unavailable after a stale token caused `Unauthorized` responses during an otherwise active logged-in session.
- Fixed the dashboard hero CTA so `Get Started` is hidden for already authenticated users.
- Fixed incognito rendering on `About`, `Contact`, and `Gallery` so those pages no longer fall back to a light-theme appearance.

### Security (if applicable)
- No security changes in this release.

### Internal
- Documented the latest dashboard, auth-retry, and public-page visual adjustments in the release changelog.
