# v0.2.6 - 2026-04-14

(D) 2026-04-14

### Added
- Added a shared wasNotificationEdited(createdAt, updatedAt) helper in app/lib/notifications-api.ts to determine whether a notification should display as edited.

### Changed
- Refactored notification UI usage across dashboard, profile, and notifications pages to rely on the shared edited-state helper instead of checking updatedAt directly.

- Updated admin navigation links on app/admin/notifications/page.tsx and app/admin/users/page.tsx to use the ArrowLeft icon component for the “Back to Admin Dashboard” action.

- Updated app/privacy/page.tsx and app/terms/page.tsx to render the effective date dynamically instead of using a hardcoded date.

- Removed the phone number field and related validation from app/signup/page.tsx.
- Cleaned up formatting and unused/commented UI in app/reminders/page.tsx.

### Fixed
- Fixed notification “Edited” labeling so it only appears when updatedAt is actually later than createdAt, preventing false edited states.
- Resolved the notification updated-at handling issue across user-facing notification surfaces.

### Security
-

### Internal
General code refactoring and cleanup across affected pages for consistency and maintainability.