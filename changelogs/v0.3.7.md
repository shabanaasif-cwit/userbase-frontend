(D) 2026-04-30

### Added
- Added a shared socket connection layer for authenticated sessions using `socket.io-client`.
- Added shared browser sync events for notifications and reminders so pages can respond to realtime updates consistently.
- Added direct navigation from the dashboard stat cards to the related notifications and reminders pages.


### Changed
- Updated the app shell to initialize realtime sync while a user is signed in.
- Updated the header and dashboard to refresh notification and reminder counts when socket sync events are received.
- Updated the admin notifications, user notifications, and reminders pages to reload their data when related realtime events arrive.
- Updated the dashboard hero stats so notification and reminder summary cards behave like interactive links.
- Changed dashboard stat cards to show pointer cursor and hover feedback to better signal clickability.

### Fixed
- Fixed notification and reminder views becoming stale until a manual refresh after create, update, delete, or read actions happened elsewhere.
- Fixed unread counters falling out of sync across dashboard, header, and detail pages during active sessions.
- Fixed dashboard summary cards so users can open the notifications or reminders pages directly from the totals section.


### Security (if applicable)
- No security changes in this release.

### Internal
- Added the frontend socket client dependency and documented the realtime sync release work in the changelog.
