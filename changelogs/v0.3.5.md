# v0.3.5
(D) 2026-04-28

### Added
- Added a shared `notification-read-updated` browser event for notification read-state sync in the current tab.

### Changed
- Updated the header notifications flow to listen for notification read-state updates and refresh the unread count immediately.

### Fixed
- Fixed the header notification unread count so it updates instantly when a user marks notifications as read, without requiring navigation to another page.

### Security (if applicable)
- No security changes in this release.

### Internal
- Reused the existing notification read-persistence layer to broadcast same-tab updates without changing unrelated notification behavior.
    