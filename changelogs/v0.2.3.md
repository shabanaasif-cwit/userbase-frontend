# v0.2.3 - 2026-04-10

(D) 2026-04-10

### Added
- Added `cursor-pointer` styling to the dashboard's `Quick tour` trigger and `Got it` dialog action for clearer interactive affordance.

### Changed
- Updated the Contact page to remove the embedded contact form and leave the page focused on direct contact information cards.

### Fixed
- Reduced misleading contact-page UI behavior by removing the in-page form component that is no longer rendered from `app/contact/page.tsx`.

### Security (if applicable)
- Not applicable for this release.

### Internal
- Removed the unused `app/component/contact-form.tsx` component file.
- Added clarifying inline comments in `app/lib/auth-context.tsx` around invalid-user and failed-auth response handling during session restoration and signup payload preparation.
