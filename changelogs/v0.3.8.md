## (D) 2026-05-06

### Added
- Added JSON request bodies to notification and reminder action requests so `POST`, `PATCH`, and `DELETE` actions are visible under Network request payload in DevTools.

### Changed
- Updated notification/reminder API helpers in `app/lib/notifications-api.ts` to include identifier fields in the request body for action-based calls.

### Fixed
- Fixed missing request payload visibility in browser DevTools for notification/reminder action requests.

### Security (if applicable)
- None.

### Internal
- Modified only `app/lib/notifications-api.ts`.
