# v0.2.7 - 2026-04-14

(D) 2026-04-14

### Added
- **`app/lib/auth-context.tsx`:** Optional `firstName` and `lastName` on `ManagedUser`, populated from the users API response in `fetchUsersFromAPI`.
- **`app/admin/users/page.tsx`:** `getUserDisplayName(user)` helper that prefers `firstName` + `lastName`, then `name`, then `email`.

### Changed
- **`app/admin/users/page.tsx`:** Search, the name column, and the edit dialog summary now use `getUserDisplayName()` instead of only `user.name || user.email`, so split names from the API show consistently.

### Fixed
-

### Security (if applicable)
-

### Internal
-
